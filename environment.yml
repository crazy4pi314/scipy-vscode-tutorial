name: tutorial
channels:
  - conda-forge
dependencies:
  - python=3.11
  - ipykernel # required for jupyter notebook in VS Code
  - notebook
  - pytest
  - matplotlib
  - numpy
  - pandas
  - regex
  - pip  # if you need more packages installed from pip 
  - pip:
    - emoji
    - myst-nb
    - jupyter-book
